## Deployment rules
- NEVER run `make deploy`, `pnpm deploy`, or any deployment command unless the user explicitly says "deploy" or "push and deploy".
- When the user says "git commit" or just "commit", ONLY commit. Do not deploy.
- When in doubt, ask before deploying.
